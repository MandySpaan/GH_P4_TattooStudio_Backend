import { Request, Response } from "express";

export const register = (req: Request, res: Response) => {
  res.send("Register code to be written");
};

export const login = (req: Request, res: Response) => {
  res.send("Login code to be written");
};
